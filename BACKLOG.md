# Backlog

Ideas and planned work not yet implemented.

## Usage tracking / request metrics

**Status:** Done — see `CHANGELOG.md`. Implemented `GET /api/stats` (total
links, total visits) and a Prometheus `/metrics` endpoint via
`prometheus-fastapi-instrumentator`.
