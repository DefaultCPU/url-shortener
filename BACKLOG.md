# Backlog

Ideas and planned work not yet implemented.

## Usage tracking / request metrics

**Status:** Done — see `CHANGELOG.md`. Implemented `GET /api/stats` (total
links, total visits) and a Prometheus `/metrics` endpoint via
`prometheus-fastapi-instrumentator`.

## Frontend polish pass

**Status:** Done — see `CHANGELOG.md`. Realistic background animation
strings, a second accent color separating action from status, and visual
feedback on shorten (result flash) and stat updates (counter pulse).
